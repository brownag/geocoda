% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics_advanced.R
\name{gc_compute_variogram_cv}
\alias{gc_compute_variogram_cv}
\title{Variogram Cross-Validation}
\usage{
gc_compute_variogram_cv(
  data,
  coords_cols,
  ilr_cols,
  n_subsets = 5,
  subset_fraction = 0.8,
  variogram_model = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame with coordinates (x, y) and ILR values}

\item{coords_cols}{Character vector of length 2: names of x, y columns}

\item{ilr_cols}{Character vector: names of ILR columns}

\item{n_subsets}{Numeric, number of CV folds (default 5)}

\item{subset_fraction}{Numeric, fraction of data per fold (default 0.8)}

\item{variogram_model}{Formula or vgm object, variogram model to test}

\item{verbose}{Logical, print progress (default TRUE)}
}
\value{
A list with elements:
\itemize{
\item \code{cv_fits}: List of fitted variograms per fold
\item \code{cv_metrics}: Data frame with RMSE per fold and component
\item \code{overall_rmse}: Mean RMSE across folds
\item \code{stability}: Standard deviation of fold-specific RMSEs
}
}
\description{
Assess variogram fit quality using subsampling and refitting strategies.
Tests whether the fitted variogram model generalizes to independent subsets of data.
}
\examples{
\dontrun{
vgm_cv <- gc_compute_variogram_cv(
  data = data_ilr,
  coords_cols = c("x", "y"),
  ilr_cols = c("ilr1", "ilr2"),
  n_subsets = 5
)

cat("Variogram model stability (across folds):\n")
print(vgm_cv$cv_metrics)
}

}
