% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical_zones.R
\name{gc_define_hierarchy}
\alias{gc_define_hierarchy}
\title{Define Hierarchical Zone Structure}
\usage{
gc_define_hierarchy(
  zone_names,
  zone_boundaries = NULL,
  parent_region = NULL,
  component_names,
  target_sum = 100
)
}
\arguments{
\item{zone_names}{Character vector, names of zones (e.g., "zone_1", "zone_2")}

\item{zone_boundaries}{Spatial object (sf) or list of spatial polygons defining zone extent}

\item{parent_region}{Character or NULL, parent region name for all zones (optional)}

\item{component_names}{Character vector, names of components (e.g., c("sand", "silt", "clay"))}

\item{target_sum}{Numeric, expected composition sum (default 100)}
}
\value{
An S3 object of class \code{"gc_hierarchy"} containing:
\itemize{
\item \code{zones}: Character vector of zone names
\item \code{boundaries}: Spatial boundaries per zone
\item \code{parent_region}: Parent region identifier
\item \code{components}: Component names
\item \code{target_sum}: Composition sum constraint
\item \code{n_zones}: Number of zones
\item \code{n_components}: Number of components
}
}
\description{
Specify nested spatial domains (zones within regions) for hierarchical modeling.
Zones inherit prior information from parent regions while allowing zone-specific
parameter estimation with shrinkage toward global distribution.
}
\examples{
\dontrun{
# Create hierarchy from stratification results
strata_result <- gc_identify_strata(data, n_strata = 3, plot = FALSE)

hierarchy <- gc_define_hierarchy(
  zone_names = paste0("stratum_", 1:3),
  parent_region = "study_area",
  component_names = c("sand", "silt", "clay")
)

print(hierarchy)
}

}
