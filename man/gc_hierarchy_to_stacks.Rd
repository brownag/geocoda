% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical_zones.R
\name{gc_hierarchy_to_stacks}
\alias{gc_hierarchy_to_stacks}
\title{Convert Hierarchical Results to Multilayer Stacks}
\usage{
gc_hierarchy_to_stacks(
  realizations,
  hierarchy,
  stats = c("mean", "sd", "p05", "p95"),
  verbose = TRUE
)
}
\arguments{
\item{realizations}{An object from \code{gc_sim_hierarchical()}}

\item{hierarchy}{A \code{"gc_hierarchy"} object}

\item{stats}{Character vector of statistics to compute
(default: c("mean", "sd", "p05", "p95"))}

\item{verbose}{Logical, print progress (default TRUE)}
}
\value{
A named list of SpatRasters, one per zone, with layers named
"component_statistic" (e.g., "sand_mean", "sand_sd")
}
\description{
Transform hierarchical simulation results into per-zone SpatRasters with
summarized statistics (mean, sd, quantiles) per component.
}
\examples{
\dontrun{
realizations <- gc_sim_hierarchical(fit, locations, nsim = 10)
hierarchy <- gc_define_hierarchy(
  zone_names = c("Zone1", "Zone2", "Zone3"),
  component_names = c("sand", "silt", "clay")
)
stacks <- gc_hierarchy_to_stacks(realizations, hierarchy)
zone1_stack <- stacks[["Zone1"]]
}

}
