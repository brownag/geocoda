% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maf_transform.R
\name{gc_inverse_maf}
\alias{gc_inverse_maf}
\title{Inverse MAF Transformation}
\usage{
gc_inverse_maf(maf_data, maf_object, ilr_mean = NULL)
}
\arguments{
\item{maf_data}{Data frame containing MAF columns (maf1, maf2, ...)}

\item{maf_object}{List from \code{\link[=gc_compute_maf]{gc_compute_maf()}} containing rotation_matrix}

\item{ilr_mean}{Vector of ILR means (optional; used for centering if provided)}
}
\value{
Data frame with new columns ilr1, ilr2, ... appended to original data
}
\description{
Back-transform from MAF space to ILR coordinates.
}
\details{
Applies: Y = R^(-1) * Z + μ
where Z are MAF factors and R is the MAF rotation matrix.

The inverse exists if the rotation matrix is invertible (which it always is
for orthogonal MAF rotations).
}
\examples{
\dontrun{
# Back-transform simulated MAF factors to ILR
maf_sims <- data.frame(maf1 = rnorm(20), maf2 = rnorm(20))
ilr_sims <- gc_inverse_maf(maf_sims, maf_obj, ilr_mean = c(0.1, -0.2))
head(ilr_sims)  # Now has ilr1, ilr2 columns
}

}
