% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sda_integration.R
\name{gc_optimize_sda_queries}
\alias{gc_optimize_sda_queries}
\title{Spatial Query Optimization}
\usage{
gc_optimize_sda_queries(
  extent,
  tile_size = NULL,
  n_workers = NULL,
  timeout_per_tile = 30,
  fallback_cache = TRUE,
  priority = "speed",
  verbose = TRUE
)
}
\arguments{
\item{extent}{Spatial object defining query area}

\item{tile_size}{Numeric, side length of square tiles in map units (default: auto)}

\item{n_workers}{Numeric, number of parallel workers (default: auto-detect)}

\item{timeout_per_tile}{Numeric, timeout per tile in seconds (default 30)}

\item{fallback_cache}{Logical, use cached results if query fails (default TRUE)}

\item{priority}{Character: "speed" (default), "completeness", or "balanced"}

\item{verbose}{Logical, print progress (default TRUE)}
}
\value{
A data frame of optimized query results combining:
\itemize{
\item All successful tile queries
\item Any fallback cached data
\item Metadata on query performance
\item Attribute: total_query_time
}
}
\description{
Optimize SDA spatial queries by splitting large extents into tiles,
parallelizing queries, implementing smart caching, and fallback strategies.
}
\details{
Optimization strategies:
\itemize{
\item \strong{Tiling}: Split large extents into tiles; query and combine independently
\item \strong{Parallelization}: Use multiple workers for concurrent tile queries
\item \strong{Smart caching}: Cache intermediate tile results for reuse
\item \strong{Fallback}: Use cached results if live query fails (maintain robustness)
\item \strong{Adaptive timeout}: Increase timeout for slow/large tiles
}
}
\examples{
\dontrun{
large_extent <- st_bbox(c(xmin = -93.6, ymin = 41.4,
                         xmax = -93.3, ymax = 41.7), crs = 4326)

results <- gc_optimize_sda_queries(
  extent = large_extent,
  n_workers = 4,
  priority = "speed"
)
}

}
