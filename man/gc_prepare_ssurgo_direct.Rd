% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_pipeline.R
\name{gc_prepare_ssurgo_direct}
\alias{gc_prepare_ssurgo_direct}
\title{Prepare SSURGO Data for Simulation (SDA Direct Interface)}
\usage{
gc_prepare_ssurgo_direct(
  map_unit_keys,
  depth_range = c(0, 30),
  weight_method = "comppct",
  target_sum = 100,
  soilDB_installed = TRUE
)
}
\arguments{
\item{map_unit_keys}{Character vector of SSURGO map unit keys (mukey values)
to query via SDA}

\item{depth_range}{Numeric vector c(min_depth, max_depth) in cm to filter
horizons (default: c(0, 30) for surface horizon)}

\item{weight_method}{Character, component weighting method
("comppct", "area", or "custom")}

\item{target_sum}{Numeric, target composition sum (default 100)}

\item{soilDB_installed}{Logical, whether to check for soilDB availability
(default TRUE; set FALSE if calling from already-soilDB-aware context)}
}
\value{
A list as returned by \code{gc_ssurgo_to_params()} if successful,
or NULL with warning if soilDB is not available
}
\description{
Query Soil Data Access (SDA) API and prepare component data for simulation.
This is a convenience wrapper that assumes soilDB is installed and available.
}
\details{
This function requires the soilDB package to be installed. Users must call:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{install.packages('soilDB')
library(soilDB)
}\if{html}{\out{</div>}}

The function queries Soil Data Access via \code{soilDB::get_SDA_property()} or
similar functions to fetch component data, then passes it to
\code{gc_ssurgo_to_params()} for processing.
}
\examples{
\dontrun{
# Requires soilDB installed
params <- gc_prepare_ssurgo_direct(
  map_unit_keys = c("463168", "463171"),
  depth_range = c(0, 30),
  weight_method = "comppct"
)
}

}
