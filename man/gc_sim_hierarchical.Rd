% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchical_zones.R
\name{gc_sim_hierarchical}
\alias{gc_sim_hierarchical}
\title{Simulate Compositional Realizations from Hierarchical Model}
\usage{
gc_sim_hierarchical(
  fit,
  locations,
  nsim = 5,
  target_names = c("sand", "silt", "clay"),
  verbose = TRUE
)
}
\arguments{
\item{fit}{An object of class \code{"gc_hierarchical_fit"}}

\item{locations}{Data frame with columns x, y (coordinates) and zone (zone assignment)}

\item{nsim}{Numeric, number of realizations (default 5)}

\item{target_names}{Character vector, names for back-transformed components
(default: c("sand", "silt", "clay"))}

\item{verbose}{Logical, print progress (default TRUE)}
}
\value{
A list of length \code{nsim}, where each element is a list containing:
\itemize{
\item \code{realizations_raster}: SpatRaster (per-zone realizations for that simulation)
\item \code{zone_realizations}: List of per-zone SpatRasters
\item \code{metadata}: Realization info (zone, nsim, target_names)
}
}
\description{
Generate spatial realizations using zone-specific posterior parameters from
fitted hierarchical model. Each realization respects zone boundaries and
compositional constraints.
}
\examples{
\dontrun{
fit <- gc_fit_hierarchical_model(data_zones, priors)
locations <- data.frame(
  x = rep(c(100, 200, 300), each = 5),
  y = rep(c(100, 200, 300), 5),
  zone = rep(c("Zone1", "Zone2", "Zone3"), each = 5)
)
realizations <- gc_sim_hierarchical(
  fit = fit, locations = locations, nsim = 10
)
}

}
