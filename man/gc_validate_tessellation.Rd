% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zonal_simulation.R
\name{gc_validate_tessellation}
\alias{gc_validate_tessellation}
\title{Validate Tessellated Compositional Raster}
\usage{
gc_validate_tessellation(raster, target_sum = 100, tolerance = 0.1)
}
\arguments{
\item{raster}{A terra::SpatRaster with simulated compositional data.}

\item{target_sum}{Numeric. Target sum for compositions (default 100 for percentages).}

\item{tolerance}{Numeric. Allowed deviation from target_sum (default 0.1).}
}
\value{
A list containing:
\itemize{
\item \code{valid}: Logical, TRUE if all cells pass constraints
\item \code{summary}: Data frame with validation stats per component
\item \code{violations}: Data frame of cells that violate constraints (indices + values)
\item \code{issues}: Character vector describing validation problems
}
}
\description{
Check sum constraints and compositional validity across all cells in a
tessellated (zone-based) simulation output.
}
\details{
Validates:
\enumerate{
\item All cells have sum = target_sum +/- tolerance
\item No negative values
\item No missing values (except designated nodata)
\item Patterns consistent with compositional data
}

Reports:
\itemize{
\item Count of violations per component
\item Spatial distribution of violations
\item Suggestions for remediation (rescaling, manual inspection)
}
}
\examples{
\dontrun{
result <- gc_simulate_by_zones(zones, data, extent, resolution = 30)
validation <- gc_validate_tessellation(result)
if (!validation$valid) {
  cat("Violations detected:\n")
  print(validation$issues)
}
}

}
