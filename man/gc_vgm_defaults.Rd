% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variogram_helpers.R
\name{gc_vgm_defaults}
\alias{gc_vgm_defaults}
\title{Suggest Reasonable Variogram Default Parameters}
\usage{
gc_vgm_defaults(ilr_params, extent)
}
\arguments{
\item{ilr_params}{A list returned by \code{gc_ilr_params()}.}

\item{extent}{A numeric vector of length 4: \code{c(xmin, ymin, xmax, ymax)},
or a spatial object (sf, terra) from which extent can be extracted.}
}
\value{
A list containing:
\itemize{
\item \code{range}: Suggested range parameter (approximately 1/3 of the diagonal extent)
\item \code{nugget}: Suggested nugget ratio (typically 0.01 to 0.05 of average sill)
\item \code{mean_sill}: Mean of the diagonal covariance terms
}
}
\description{
Inspect data extent and ILR covariance structure to suggest ballpark values
for range, nugget, and sill of a variogram model. This is a convenience
function to help users get started before fine-tuning.
}
\details{
This function:
\enumerate{
\item Calculates the spatial extent diagonal (Euclidean distance from min to max)
\item Suggests range as ~1/3 of the extent diagonal
\item Calculates mean of diagonal covariance terms as representative sill
\item Suggests nugget as 1\% of the mean sill
}

These are heuristics and should be refined using empirical variography
(see \code{\link[=gc_fit_vgm]{gc_fit_vgm()}}).
}
\examples{
samples <- data.frame(
  sand = c(20, 25, 30, 22),
  silt = c(60, 55, 50, 58),
  clay = c(20, 20, 20, 20)
)

params <- gc_ilr_params(samples)
extent <- c(0, 0, 100, 100)

suggestions <- gc_vgm_defaults(params, extent)
print(suggestions)

}
